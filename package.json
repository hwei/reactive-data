{
  "name": "reactive-data-workspace",
  "version": "1.0.0",
  "description": "Reactive data libraries workspace",
  "private": true,
  "workspaces": [
    "reactive-data",
    "reactive-data-solid",
    "reactive-data-patch"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test:run --workspaces",
    "lint": "npm run lint --workspaces",
    "type-check": "npm run type-check --workspaces"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@typescript-eslint/eslint-plugin": "^6.13.0",
    "@typescript-eslint/parser": "^6.13.0",
    "@vitest/ui": "^1.0.0",
    "eslint": "^8.54.0",
    "typescript": "^5.3.0"
  }
}
